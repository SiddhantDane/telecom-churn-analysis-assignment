# Data

The raw dataset (`customer_churn_dataset-training-master.csv`) is the Customer
Churn Dataset provided with the assignment brief. It is not included here due to
file size. Place it in this folder before running the scripts.

`churn_for_tableau.csv` is generated by running the Python scripts.
